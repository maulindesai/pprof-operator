---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: profilers.observability.pprof-operator.dev
spec:
  group: observability.pprof-operator.dev
  names:
    kind: Profiler
    listKind: ProfilerList
    plural: profilers
    singular: profiler
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: Profiler is the Schema for the profilers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Profiler
            properties:
              awsCredentialsSecret:
                description: AWS credentials secret name
                type: string
              cpuThreshold:
                description: CPU threshold in percentage (0-100) that triggers profiling
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              memoryThreshold:
                description: Memory threshold in percentage (0-100) that triggers
                  profiling
                format: int32
                maximum: 100
                minimum: 0
                type: integer
              profileDuration:
                default: 30
                description: Duration in seconds to collect the profile
                format: int32
                type: integer
              s3Bucket:
                description: S3 bucket to upload profiles to
                type: string
              s3PathPrefix:
                description: S3 path prefix for storing profiles
                type: string
              s3Region:
                default: us-east-1
                description: S3 region
                type: string
              selector:
                description: Selector to find pods to monitor
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              targetContainer:
                description: Target container to monitor
                type: string
            required:
            - s3Bucket
            - targetContainer
            type: object
          status:
            description: status defines the observed state of Profiler
            properties:
              profiles:
                description: List of profiles that have been collected
                items:
                  description: ProfileRecord contains information about a collected
                    profile
                  properties:
                    reason:
                      description: Reason why the profile was triggered
                      type: string
                    s3URL:
                      description: S3 URL where the profile is stored
                      type: string
                    timestamp:
                      description: Timestamp when the profile was collected
                      format: date-time
                      type: string
                    type:
                      description: Type of profile (cpu, memory, etc.)
                      type: string
                  required:
                  - reason
                  - s3URL
                  - timestamp
                  - type
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
